SELECT
            count( ra.id )
        FROM
        ( SELECT id FROM resold_apartment WHERE del_flag = 0 AND audit_status = 'pass' AND user_id = #{id} GROUP BY city ) ra
	
	
	
	
	SELECT
            count(id)
        FROM
            resold_apartment
        WHERE del_flag = 0
        AND user_id = #{id}
        AND sell_status = #{status}
	
	
	
	
	
	
	SELECT
            count(id)
        FROM
            resold_apartment
        WHERE del_flag = 0
        AND user_id = #{id}
        AND sell_status = #{status}
	
	
	
	
	SELECT
            u.id AS user_id,
            u.nick_name AS user_name,
            u.photo AS user_photo,
            im.identifier AS identifier,
            im.user_sig AS user_sig
        FROM
            user u
        LEFT JOIN im_user im ON u.id = im.user_id
            WHERE u.is_delete = 0 AND u.id = #{userId}
